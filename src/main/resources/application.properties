# Server configuration - use PORT env variable for cloud deployment
server.port=${PORT:8080}

# JSP configuration
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# Production settings
spring.profiles.active=prod

# JSP settings for production
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true

# Disable JSP caching in development (enable in production)
server.servlet.jsp.init-parameters.development=false

# Email configuration - use environment variables for security
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:sahoodhreetikumar@gmail.com}
spring.mail.password=${MAIL_PASSWORD:igxb qrha jmta pbda}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
